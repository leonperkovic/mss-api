# Mars Scavenging Station API

## Setup
1. Start dependencies:
    ```
   cd mss-services && docker-compose up -d & cd -
   ```
2. Build project:
    ```
   ./gradlew clean build
   ```
3. Run with default profiles
